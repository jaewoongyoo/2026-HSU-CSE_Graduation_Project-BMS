## 1. 시스템 아키텍처 개요

프로젝트는 **3-tier** 구조로 분리되어 있다.

```
Android App (Kotlin)
    ↓ REST API
Backend Server (FastAPI + SQLAlchemy)
    ↓ REST API (HTTP)
AI Server (FastAPI + 곡선 피팅 기반 SOH 예측)
```

| 레이어 | 경로 | 핵심 기술 |
|---|---|---|
| Android 앱 | `android/` | Kotlin, Jetpack Compose, Navigation, BatteryManager API |
| Backend 서버 | `backend/` | FastAPI, SQLAlchemy, PostgreSQL, passlib(bcrypt) |
| AI 서버 | `ai/soh_service/` | FastAPI, SciPy(curve_fit), NASA+CALCE로 피팅한 표준 열화 곡선 |

---

## 2. Android 앱

### 2-1. 화면 구성 및 네비게이션

`MainActivity.kt`에서 Jetpack Navigation Compose로 전체 흐름을 관리한다.

```
splash
  ├─ (로그인 상태 O) → home
  └─ (로그인 상태 X) → login
                          └─ signup
home
  ├─ 기기 선택 → dashboard/{deviceNickname}
  └─ 기기 추가 → landing → dashboard/{deviceNickname}
```

| 화면 | 라우트 | 주요 역할 |
|---|---|---|
| SplashScreen | `splash` | 자동 로그인 여부 확인 후 home/login 분기 |
| LoginScreen | `login` | 사용자 로그인 처리 |
| SignupScreen | `signup` | 회원가입 처리 |
| HomeScreen | `home` | 등록 기기 목록 표시 / 기기 삭제 / 로그아웃 |
| LandingScreen | `landing` | 신규 보조배터리 등록 (정보 입력) |
| DashboardScreen | `dashboard/{deviceNickname}` | 실시간 배터리 모니터링 |

### 2-2. 데이터 모델

#### BatteryDevice
사용자가 등록한 보조배터리 정보.

| 필드 | 타입 | 설명 |
|---|---|---|
| `brand` | String | 제조사 (예: Anker, Samsung) |
| `nickname` | String | 사용자 지정 별칭 (PK 역할, 필수) |
| `capacity` | Int | 정격 용량 (mAh, 0 초과 필수) |
| `manufactureDate` | String | 제조년월 (형식: YYYY-MM) |

> 향후 추가 예정: `ageInMonths`, `chargeCount`, `healthPercentage`, `estimatedLifespan`

#### BatteryStatus
실시간 배터리 상태 스냅샷.

| 필드 | 타입 | 설명 |
|---|---|---|
| `soc` | Int | 잔량 (%) |
| `voltage` | Float | 전압 (V) |
| `current` | Float | 전류 (mA), 충전 중 양수 |
| `temperature` | Float | 온도 (°C) |
| `isCharging` | Boolean | 충전 중 여부 |
| `remainingTime` | Long | 완충 예상 시간 (분), 미산출 시 -1 |

### 2-3. DashboardViewModel — 실시간 데이터 수집

Android `BatteryManager` API를 통해 **2초 주기**로 하드웨어 센서 데이터를 수집한다.

| 수집 항목 | Android API | 변환 |
|---|---|---|
| 잔량 (SOC) | `EXTRA_LEVEL / EXTRA_SCALE` | level / scale × 100 |
| 전압 | `EXTRA_VOLTAGE` | mV ÷ 1000 → V |
| 온도 | `EXTRA_TEMPERATURE` | 0.1°C 단위 ÷ 10 → °C |
| 전류 | `BATTERY_PROPERTY_CURRENT_NOW` | μA ÷ 1000 → mA |
| 충전 상태 | `EXTRA_STATUS` | CHARGING / FULL → true |
| 완충 예상 시간 | `computeChargeTimeRemaining()` | ms ÷ 60000 → 분 (API 28+) |

- 완충 예상 시간이 -1이면 UI에서 "계산 중..." 표시
- `viewModelScope.launch` + `delay(2000)` 루프로 구현

### 2-4. 저장소 구조

| 클래스 | 역할 |
|---|---|
| `PreferenceManager` | 기기 목록(BatteryDevice) SharedPreferences 관리 |
| `UserManager` | 로그인 상태, 사용자 정보 SharedPreferences 관리 |
| `BatteryRepository` (interface) | 배터리 상태/기기 CRUD 추상화 |
| `MockBatteryRepository` | 테스트용 Mock 구현체 |

### 2-5. 기술 스택 (build.gradle.kts)

| 항목 | 버전/값 |
|---|---|
| compileSdk / targetSdk | 35 |
| minSdk | 26 (Android 8.0) |
| Compose BOM | 최신 |
| Navigation Compose | 2.8.9 |
| ViewModel Compose | 2.8.7 |
| kotlinx-serialization-json | 1.6.2 |

---

## 3. Backend 서버

### 3-1. API 엔드포인트 목록

Base URL: `/api/v1`

#### 인증 (`/auth`)

| 엔드포인트 | 메서드 | 요청 | 응답 | 설명 |
|---|---|---|---|---|
| `/auth/signup` | POST | `SignUpRequest` | `SignUpResponse` | 회원가입 |
| `/auth/login` | POST | `LoginRequest` | `LoginResponse` | 로그인 |

#### 사용자 (`/users`)

| 엔드포인트 | 메서드 | 설명 |
|---|---|---|
| `/users` | GET | 사용자 목록 조회 (skip, limit 파라미터) |
| `/users/{user_id}` | GET | 단일 사용자 조회 |
| `/users/{user_id}` | PATCH | 사용자 정보 수정 |
| `/users/{user_id}` | DELETE | 사용자 삭제 |

#### 충전 세션 (`/sessions`)

| 엔드포인트 | 메서드 | 요청 | 응답 | 설명 |
|---|---|---|---|---|
| `/sessions/start` | POST | `SessionStartRequest` | `SessionStartResponse` | 세션 시작, session_id 발급 |
| `/sessions/{id}/finish` | POST | `SessionFinishRequest` | `SessionFinishResponse` | 세션 종료, capacity_ah 기록 |
| `/sessions/{id}/raw` | POST | `RawUploadRequest` | - | 원시 배터리 데이터 업로드 |
| `/sessions/{id}/predict-soh` | POST | - | `SohPredictResponse` | AI 서버에 SOH 예측 요청 |
| `/sessions/{id}/result` | GET | - | `SessionResultResponse` | 세션 분석 결과 조회 |

#### 헬스체크

| 엔드포인트 | 메서드 | 설명 |
|---|---|---|
| `/health` | GET | 서버 상태 확인 |

### 3-2. 주요 스키마

#### SessionStartRequest
```
user_id, device_model, android_api_level,
powerbank_id (optional), cable_id (optional),
phone_capacity_mah (default: 4000),
powerbank_capacity_mah (default: 10000),
session_start_ts,
start_battery_level_pct     ← 신규. Android가 전송. Backend는 battery_sessions.powerbank_capacity_start_mah 컬럼에 저장
```

> **⚠️ AI 팀 → Android / Backend 팀 요청 사항**
>
> `/soh/predict/multi`의 개인화 SOH 예측(노화 속도 기울기 계산)을 사용하려면
> 각 세션의 "스마트폰 충전 시작 시점 배터리 잔량(%)"이 필요하다. 다음 추가를 요청한다:
>
> - Android: `/sessions/start` 요청 body에 `start_battery_level_pct` (FLOAT, 0~100) 필드를 추가해 전송.
>   값은 `BatteryManager.EXTRA_LEVEL / EXTRA_SCALE × 100`으로 계산한 SOC %.
> - Backend: 수신한 `start_battery_level_pct` 값을 DB의
>   `battery_sessions.powerbank_capacity_start_mah` 컬럼에 그대로 저장
>   (컬럼명은 명세서 레거시, 저장 값은 SOC %).

#### SessionFinishRequest
```
session_end_ts, capacity_ah (optional),
end_battery_level_pct       ← 신규. Android가 전송. Backend는 battery_sessions.powerbank_capacity_end_mah 컬럼에 저장
```

> **⚠️ AI 팀 → Android / Backend 팀 요청 사항**
>
> 로드맵 `/soh/degradation/forecast` (6개월 열화 예측)에서 EFC 계산에 사용된다.
> 현재 AI 엔드포인트에서는 미사용이지만, 스키마 확장은 같이 진행 권장.
>
> - Android: `/sessions/{id}/finish` 요청 body에 `end_battery_level_pct` (FLOAT, 0~100) 필드 추가.
> - Backend: 수신한 값을 `battery_sessions.powerbank_capacity_end_mah` 컬럼에 저장 (값은 SOC %).

#### RawDataPoint
```
timestamp, voltage_mv, current_ma, temperature_c, elapsed_ms,
battery_level (optional), battery_status (optional), screen_state (optional)
```
- `elapsed_ms`는 단조 증가 검증 (validator 포함)

#### SohPredictResponse
```
soh_percentage, condition, estimated_full_charges,
powerbank_usable_mah, mean_temperature_c,
standard_soh_percentage, degradation_rate_ratio,
sessions_used, sessions_total, confidence
```

> **⚠️ AI 팀 → Backend 팀 요청 사항**
>
> AI 서버가 곡선 피팅 방식으로 전환되면서 응답에 5개 신규 필드가 추가되었다.
> `SohPredictResponse` Pydantic 스키마에 다음 Optional 필드를 추가해
> AI 응답을 그대로 Android에 전달할 수 있게 해달라.
>
> - `standard_soh_percentage: Optional[float]`
> - `degradation_rate_ratio: Optional[float]`
> - `sessions_total: Optional[int]`
> - `confidence: Optional[str]`  (`fallback` / `low` / `medium` / `high`)
>
> `sessions_used`는 기존에 이미 있으므로 유지. `smartphone_received_mah` 필드는
> AI 서버가 항상 `0.0`을 반환하므로 **[Deprecated]** 처리 (스키마에서는 제거 가능).

### 3-3. DB 스키마

#### `users`

사용자 계정 및 스마트폰 정보를 담는 테이블.

| 컬럼 | 타입 | NULL | 설명 |
|---|---|---|---|
| `id` | SERIAL | NULL | 사용자 식별자. 자동 1씩 증가 |
| `username` | VARCHAR(100) | NOT NULL | 로그인 아이디 |
| `password_hash` | VARCHAR(255) | NOT NULL | 비밀번호. DB에 해시값으로 저장 |
| `phone_model` | VARCHAR(100) | NULL | 사용자 스마트폰 기종. 회원가입 시 프론트에서 전달 |
| `phone_uid` | VARCHAR(100) | NULL | 스마트폰 식별 고유값. 프론트가 생성해서 백엔드에 전달 |
| `created_at` | TIMESTAMP | NULL | 계정 생성 시각 |

#### `devices`

사용자가 등록한 보조배터리 정보를 담는 테이블. 스마트폰 정보는 `users`에 있으므로 여기서는 보조배터리 정보만 관리.

| 컬럼 | 타입 | NULL | 설명 |
|---|---|---|---|
| `id` | SERIAL | NULL | 보조배터리 식별자. 자동 1씩 증가 |
| `user_id` | INTEGER | NOT NULL | 어떤 사용자의 보조배터리인지. `users.id` FK |
| `manufacturer` | VARCHAR(100) | NULL | 보조배터리 제조사 |
| `model_name` | VARCHAR(100) | NOT NULL | 보조배터리 모델명 |
| `powerbank_capacity_mah` | INTEGER | NULL | 출시 당시 정격 용량 (mAh). 사용자가 직접 입력. SOH 계산의 분모로 사용되므로 오입력 시 SOH 전체에 영향 |
| `manufacture_date` | VARCHAR(20) | NULL | 보조배터리 제조년월 |
| `created_at` | TIMESTAMP | NULL | 기기 등록 시각 |

#### `battery_sessions`

10분짜리 충전 세션의 메타데이터를 저장하는 테이블.

> **설계 이슈**: `id`와 `session_id`가 동일한 역할을 하는 것으로 보임. 백엔드가 `id` 대신 `session_id`를 식별자로 사용하는 구조로 설계한 것으로 추정.

| 컬럼 | 타입 | NULL | 설명 |
|---|---|---|---|
| `id` | SERIAL | NULL | 레코드 식별자 (자동 증가 PK) |
| `session_id` | VARCHAR(100) | NOT NULL | 세션 고유 식별자. `battery_telemetry.session_id`에서 참조됨 |
| `device_id` | INTEGER | NOT NULL | 보조배터리 식별자. `devices.id` FK |
| `user_id` | INTEGER | NOT NULL | 사용자 식별자. `users.id` FK |
| `android_api_level` | INTEGER | NOT NULL | 세션 측정 시 사용된 Android API 레벨 |
| `powerbank_id` | VARCHAR(100) | NULL | **백엔드팀 확인 필요** — `device_id`로 이미 보조배터리를 식별하는데 이 필드가 별도로 존재하는 이유 불명확 |
| `session_start_ts` | TIMESTAMP | NOT NULL | 세션 시작 시각 |
| `session_end_ts` | TIMESTAMP | NULL | 세션 종료 시각 |
| `capacity_ah` | FLOAT | NULL | 해당 세션 동안 스마트폰이 파워뱅크로부터 받은 에너지량 (Ah) |
| `powerbank_capacity_start_mah` | FLOAT | NOT NULL | 세션 시작 시점의 스마트폰 배터리 SOC (%). **저장 값 단위는 mAh가 아니라 SOC 퍼센트(0~100)**. 필드명은 레거시 유지. AI 개인화 예측에 사용. **필드명 변경 권장: `soc_start_pct`** |
| `powerbank_capacity_end_mah` | FLOAT | NULL | 세션 종료 시점의 스마트폰 배터리 SOC (%). 저장 값 단위는 SOC 퍼센트. 현재 AI 엔드포인트 미사용, 4-8 로드맵 `/soh/degradation/forecast` EFC 계산용. **필드명 변경 권장: `soc_end_pct`** |
| `status` | VARCHAR(30) | NOT NULL | **백엔드팀 확인 필요** — 충전 상태로 추정 |
| `label_capacity_ah` | FLOAT | NULL | 전력계로 직접 측정한 파워뱅크의 실제 용량 (Ah). 학습 데이터 수집 시 AI팀이 수동 입력; 일반 서비스 데이터는 null |
| `created_at` | TIMESTAMP | NULL | 레코드 생성 시각 |
| `updated_at` | TIMESTAMP | NULL | 레코드 수정 시각 |

> **⚠️ AI 팀 → Backend 팀 요청 사항 (개인화 예측용 DB 구현)**
>
> 본 명세서에는 `powerbank_capacity_start_mah` / `powerbank_capacity_end_mah` 필드가
> 정의되어 있으나, **실제 DB (`backend/app/db/models.py`)에는 아직 구현되지 않은 상태**다.
> `/soh/predict/multi`의 개인화 SOH 예측(노화 속도 기울기 계산)을 사용하려면
> 이 두 필드를 실제 DB 컬럼으로 추가해야 한다.
>
> **요청 사항:**
>
> - `battery_sessions` 테이블에 다음 컬럼을 Alembic migration으로 실제 추가:
>   - `powerbank_capacity_start_mah FLOAT NOT NULL` (값: SOC %, 0~100)
>   - `powerbank_capacity_end_mah FLOAT NULL` (값: SOC %, 0~100)
> - 컬럼명은 레거시이지만 **저장 값의 단위는 SOC 퍼센트**라는 점을 주석으로 명시.
> - Backend는 AI 서버 `/soh/predict/multi` 호출 시,
>   DB의 `powerbank_capacity_start_mah` 값을 AI 계약 필드 `start_battery_level_pct`에 그대로 매핑해 전달.
>
> **현재 `/soh/predict` (단일 세션)만 쓴다면 이 요청은 보류 가능**하나,
> 개인화 기능을 활성화하려면 필수.

> **역추적 계산 예시**: 세션 동안 스마트폰 SOC가 20% → 40%로 변했고 `capacity_ah` = 2Ah이면, 스마트폰 현재 용량 추정 = 2Ah / 0.2 = 10Ah

#### `battery_telemetry`

스마트폰이 파워뱅크로 충전되는 동안 프론트가 2초 간격으로 측정한 시계열 데이터를 저장하는 테이블.

> **설계 이슈**: `session_id` 필드가 `battery_sessions.id`(PK)가 아닌 `battery_sessions.session_id`를 참조하고 있음.

| 컬럼 | 타입 | NULL | 설명 |
|---|---|---|---|
| `id` | SERIAL | NULL | 레코드 식별자 |
| `device_id` | INTEGER | NOT NULL | 보조배터리 식별자. `devices.id` FK |
| `session_id` | VARCHAR(100) | NOT NULL | 세션 식별자. `battery_sessions.session_id` FK |
| `timestamp` | TIMESTAMP | NOT NULL | **백엔드팀 확인 필요** |
| `soc` | FLOAT | NULL | 스마트폰 배터리 잔량 (%) |
| `voltage` | FLOAT | NULL | 스마트폰 배터리 전압 (V) |
| `current_ma` | FLOAT | NULL | 스마트폰 배터리 전류 (mA) |
| `temperature_c` | FLOAT | NULL | 스마트폰 배터리 온도 (°C) |
| `power_w` | FLOAT | NULL | **백엔드팀 확인 필요** |
| `elapsed_ms` | FLOAT | NULL | 세션 시작 후 경과 시간(ms) |
| `screen_state` | BOOLEAN | NULL | **백엔드팀 확인 필요** |
| `created_at` | TIMESTAMP | NULL | 레코드 생성 시각 |

#### `battery_ai_results`

AI API 호출 결과를 저장하는 테이블. 단일 세션 및 다중 세션 예측 모두 동일한 스키마로 저장한다.

| 컬럼 | 타입 | NULL | 설명 |
|---|---|---|---|
| `id` | SERIAL | NOT NULL | 레코드 식별자 |
| `session_id` | VARCHAR(100) | NOT NULL | 세션 식별자. `battery_sessions.session_id` FK |
| `soh_percentage` | FLOAT | NOT NULL | 추정 SOH (%) |
| `condition` | VARCHAR(20) | NOT NULL | 상태 라벨. `우수` ≥90 / `양호` ≥80 / `주의` ≥70 / `교체 권장` <70 |
| `estimated_full_charges` | FLOAT | NOT NULL | 현재 용량으로 스마트폰 완충 가능 횟수 |
| `powerbank_usable_mah` | FLOAT | NOT NULL | 실사용 가능 용량 (mAh). `powerbank_capacity × SOH × 0.85` |
| `mean_temperature_c` | FLOAT | NULL | 세션 평균 온도. 온도 데이터 없으면 null |
| `standard_soh_percentage` | FLOAT | NOT NULL | 표준 곡선 기준 SOH (%). 개인화 전 참조값 |
| `degradation_rate_ratio` | FLOAT | NOT NULL | 표준 대비 노화 속도 비율. 범위 `[0.3, 3.0]`. 개인화 불가 시 1.0 |
| `sessions_used` | INTEGER | NOT NULL | 필터 통과한 유효 세션 수 |
| `sessions_total` | INTEGER | NOT NULL | 요청에 포함된 전체 세션 수 |
| `confidence` | VARCHAR(20) | NOT NULL | 예측 신뢰도. `fallback` / `low` / `medium` / `high` |
| `raw_response_json` | TEXT | NULL | AI 서버 원본 응답 JSON |
| `created_at` | TIMESTAMP | NOT NULL | 레코드 생성 시각 |
| `updated_at` | TIMESTAMP | NOT NULL | 레코드 수정 시각 |

> **⚠️ AI 팀 → Backend 팀 요청 사항 (신규 응답 필드 DB 반영)**
>
> 상단 컬럼 목록의 다음 5개는 **실제 `backend/app/db/models.py::BatteryAiResult`에 아직 추가되지 않은 상태**다.
> AI 서버가 곡선 피팅 방식으로 전환되면서 응답에 추가된 필드이므로 DB 컬럼으로 반영 요청:
>
> - `standard_soh_percentage FLOAT` — 표준 곡선 기준 SOH (개인화 전 참조값)
> - `degradation_rate_ratio FLOAT` — 표준 대비 유저 노화 속도 비율 (1.0 = 표준, 범위 `[0.3, 3.0]`)
> - `sessions_used INT` — 필터 통과한 유효 세션 수
> - `sessions_total INT` — 요청에 포함된 전체 세션 수
> - `confidence VARCHAR(20)` — 예측 신뢰도: `fallback` / `low` / `medium` / `high`
>
> 기존 `smartphone_received_mah` 컬럼은 **[Deprecated]**. 곡선 피팅 방식에서는 계산되지 않고
> AI 서버가 항상 `0.0`을 반환하므로 스키마에서 제거 가능 (삭제 시 하위호환 주의).
>
> Alembic migration 생성 후 `alembic upgrade head` 실행 필요.

#### `soh_analysis`

SOH 분석 결과 및 예측값을 저장하는 테이블.

| 컬럼 | 타입 | NULL | 설명 |
|---|---|---|---|
| `id` | SERIAL | NULL | 레코드 식별자 |
| `device_id` | INTEGER | NOT NULL | 보조배터리 식별자. `devices.id` FK |
| `session_id` | VARCHAR(100) | NOT NULL | 세션 식별자. `battery_sessions.session_id` FK |
| `analyzed_at` | TIMESTAMP | NULL | 분석 시각 |
| `current_soh` | FLOAT | NULL | 현재 SOH |
| `grade` | VARCHAR(5) | NULL | 등급 |
| `degradation_rate_ratio` | FLOAT | NOT NULL | 표준 대비 노화 속도 비율. 개인화 불가 시 1.0 |
| `predicted_soh_1m` | FLOAT | NULL | 1개월 후 예측 SOH |
| `predicted_soh_3m` | FLOAT | NULL | 3개월 후 예측 SOH |
| `predicted_soh_6m` | FLOAT | NULL | 6개월 후 예측 SOH |
| `recommendation` | TEXT | NULL | 권장 사항 |

#### `shared_reports`

분석 결과 공유 링크 정보를 저장하는 테이블.

| 컬럼 | 타입 | NULL | 설명 |
|---|---|---|---|
| `id` | SERIAL | NULL | 레코드 식별자 |
| `device_id` | INTEGER | NOT NULL | 보조배터리 식별자. `devices.id` FK |
| `share_token` | VARCHAR(100) | NOT NULL | 공유 토큰 |
| `phone_model` | VARCHAR(100) | NULL | 스마트폰 기종 |
| `is_public` | BOOLEAN | NULL | 공개 여부 |
| `expires_at` | TIMESTAMP | NULL | 만료 시각 |
| `created_at` | TIMESTAMP | NULL | 레코드 생성 시각 |

### 3-4. 서비스 로직 요약

#### 세션 흐름
1. `POST /sessions/start` → `session_id` 발급 (UUID 기반, `sess_` prefix). `start_battery_level_pct` 수신 후 DB의 `powerbank_capacity_start_mah`에 저장.
2. `POST /sessions/{id}/raw` → 원시 데이터 누적 저장 (최소 10포인트 필요)
3. `POST /sessions/{id}/finish` → `capacity_ah` 및 `end_battery_level_pct` 기록 (DB의 `powerbank_capacity_end_mah`에 저장), status → `finished`
4. `POST /sessions/{id}/predict-soh` → AI 서버 `/soh/predict` 또는 `/soh/predict/multi` 호출 → 결과 DB 저장

#### AI 서버 호출 조건
- `status == "finished"`
- `capacity_ah != null`
- `raw_points >= 10개`

#### 단일 vs 다중 세션 예측 분기
- 중간고사까지는 `/soh/predict` (단일) 호출로 유지 가능. 모든 유저에게 표준 곡선 fallback 값이 반환된다.
- 개인화 기능 활성화 시 최근 N개 세션(권장 최대 20개)을 모아 `/soh/predict/multi` 호출. 각 세션에는 DB의 `powerbank_capacity_start_mah` 값을 `start_battery_level_pct`로 매핑해 전달.

### 3-5. 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | FastAPI |
| ORM | SQLAlchemy |
| DB 드라이버 | psycopg2-binary (PostgreSQL) |
| 인증 | passlib[bcrypt] |
| HTTP 클라이언트 | requests |
| 설정 관리 | pydantic-settings |

---

## 4. AI 서버

### 4-1. API 엔드포인트

Base URL: `/soh`

| 엔드포인트 | 메서드 | 설명 |
|---|---|---|
| `/soh/predict` | POST | 단일 세션 → 표준 곡선 기반 fallback 예측 |
| `/soh/predict/multi` | POST | 다중 세션 → 개인화 SOH 예측 (유저 노화 속도 반영) |
| `/soh/health` | GET | 표준 곡선 로드 상태 및 피팅 통계 확인 |

> **로드맵에만 존재**: `/soh/degradation/forecast` — 중간고사 이후 구현 예정. 아래 4-8 참조.

### 4-2. 요청/응답 스키마

#### `/soh/predict` 요청
```json
{
  "cycle_records": [
    {
      "voltage_mv": 4200.0,
      "current_ma": 1500.0,
      "temperature_c": 25.0,
      "elapsed_ms": 0.0
    }
  ],
  "powerbank_capacity_mah": 10000,
  "phone_capacity_mah": 4000
}
```
- `cycle_records` 최소 10개 필요
- `temperature_c`는 nullable
- `capacity_ah` 필드가 함께 전송되어도 무시된다 (하위호환, Pydantic `extra="ignore"`)

**동작**: 단일 세션으로는 개인화가 불가능하므로 표준 곡선의 중앙값 지점에서 SOH를 fallback으로 반환한다.
모든 유저에게 동일한 값이 나오므로, 개인화 결과가 필요하면 `/soh/predict/multi`를 사용해야 한다.

#### `/soh/predict/multi` 요청
```json
{
  "sessions": [
    {
      "cycle_records": [
        {"voltage_mv": 5000, "current_ma": 1600, "temperature_c": 27.0, "elapsed_ms": 0},
        {"voltage_mv": 5000, "current_ma": 1580, "temperature_c": 27.5, "elapsed_ms": 60000}
      ],
      "start_battery_level_pct": 20.0
    },
    {
      "cycle_records": [ /* ... */ ],
      "start_battery_level_pct": 15.0
    }
  ],
  "powerbank_capacity_mah": 10000,
  "phone_capacity_mah": 4000
}
```
- `sessions`는 오래된 세션부터 최신 순서로 배열
- 각 세션은 `cycle_records` (최소 2개) + `start_battery_level_pct` (0~100, 필수) 포함
- **`start_battery_level_pct` 값은 Backend가 DB의 `battery_sessions.powerbank_capacity_start_mah` 컬럼 값을 그대로 매핑해 전달** (저장 값 단위가 SOC %이므로 변환 불필요)
- 최근 20개 세션만 사용 (초과 시 오래된 세션 자동 제외)

**자동 필터링 조건** (다음 조건 중 하나라도 해당하면 세션 제외):

| 필터 | 임계값 |
|---|---|
| 세션 최소 길이 | 10분 (600초) |
| 세션 최소 전달 에너지 | 2 Wh |
| 시작 배터리 잔량 상한 | 85% |
| 채울 여지 하한 | 15%p (100 - start_battery_level_pct) |

유효 세션이 3개 미만이면 표준 곡선 fallback으로 대체된다 (`confidence = fallback | low`).

#### 공통 응답 (단일 · 다중 동일)
```json
{
  "soh_percentage": 82.15,
  "condition": "양호",
  "estimated_full_charges": 1.74,
  "powerbank_usable_mah": 6982.8,
  "mean_temperature_c": 28.6,
  "standard_soh_percentage": 84.23,
  "degradation_rate_ratio": 1.15,
  "sessions_used": 7,
  "sessions_total": 8,
  "confidence": "medium",
  "smartphone_received_mah": 0.0
}
```

| 필드 | 설명 |
|---|---|
| `soh_percentage` | 개인화 SOH (%). 단일 세션의 경우 표준 곡선 fallback 값과 동일 |
| `condition` | `우수` / `양호` / `주의` / `교체 권장` (아래 기준표) |
| `estimated_full_charges` | 스마트폰 완충 가능 횟수 추정 |
| `powerbank_usable_mah` | 실사용 가능 용량 (mAh). 컨버터 효율 85% 반영 |
| `mean_temperature_c` | 요청 세션들의 평균 온도 (nullable) |
| `standard_soh_percentage` | 표준 곡선 기준 SOH (%). 개인화 전 참조값 |
| `degradation_rate_ratio` | 표준 대비 노화 속도 비율 (1.0 = 표준, >1.0 = 더 빠른 노화). 범위 `[0.3, 3.0]` |
| `sessions_used` | 필터 통과한 유효 세션 수 (단일 요청은 항상 0) |
| `sessions_total` | 요청에 포함된 전체 세션 수 |
| `confidence` | 예측 신뢰도 (아래 기준표) |
| `smartphone_received_mah` | **[Deprecated]** 하위호환. 항상 `0.0` |

#### condition 기준표
| SOH 범위 | 라벨 |
|---|---|
| 90% 이상 | 우수 |
| 80% 이상 ~ 90% 미만 | 양호 |
| 70% 이상 ~ 80% 미만 | 주의 |
| 70% 미만 | 교체 권장 |

#### confidence 기준표
| confidence | 조건 | 권장 UI 표시 |
|---|---|---|
| `high` | 유효 세션 10개 이상 & 노화 신호 유의 | SOH 값 그대로 제공 |
| `medium` | 유효 세션 5~9개 & 유의, 또는 10+지만 신호 약함 | SOH 값 제공 + "추후 더 정확해질 수 있습니다" 안내 |
| `low` | 유효 세션 3~4개 & 유의, 또는 5~9지만 신호 약함 | SOH 값 제공 + "신뢰도 낮음. 몇 번 더 사용 후 재확인 권장" |
| `fallback` | 유효 세션 0~2개, 또는 단일 세션 요청 | "표준 기준값" 표시 + "개인화 데이터가 부족" 안내 |

#### `/soh/health` 응답
```json
{
  "status": "ok",
  "curve_fit_loaded": true,
  "standard_curve": {
    "dataset_scope": "nasa_only",
    "fit_point_count": 1610,
    "cell_count": 20,
    "rmse": 0.0689,
    "mae": 0.0505
  }
}
```

### 4-3. 모델 구조

| 항목 | 값 |
|---|---|
| 모델 | `CurveFitPredictor` (지수 감쇠 곡선 피팅 + 유저별 기울기 선형 회귀) |
| 수식 | `soh(x) = a · exp(-b·x) + c`. x는 셀별 정격 에너지 대비 누적 전달 에너지 비율 |
| 피팅 데이터 | NASA Battery Dataset (CALCE는 중간고사 이후 통합 예정) |
| 아티팩트 | `artifacts/curve_fit/standard_curve.json` (단일 JSON 파일) |
| 이력 | 롤링 윈도우 최근 20개 세션 (`N_MAX_SESSIONS`) |
| 출력 | SOH 비율 (0.0 ~ 1.0, clamp 적용) |

**표준 곡선 JSON 구성:**
```
{
  "params": {"a": ..., "b": ..., "c": ...},
  "fit_point_count": 1610,
  "cell_count": 20,
  "dataset_scope": "nasa_only" | "calce_only" | "nasa_calce",
  "x_median": ...,
  "x_p95": ...,
  "mean_slope_over_fit_range": ...,
  "rmse": ...,
  "mae": ...
}
```

### 4-4. 전처리 파이프라인

> 전처리는 **offline 표준 곡선 피팅 단계**에서만 수행된다. 런타임 추론은 경량 경로를 쓴다 (4-5 참조).

#### NASA/CALCE → 표준 곡선
1. canonical parquet에서 cell별로 방전 사이클을 시간 순서로 정렬
2. 각 사이클의 전달 에너지(Wh) = `∫ V·I dt` 계산 (양수 전류 구간만 적분)
3. cell별 누적 에너지를 정격 에너지(baseline_ah × 3.7V)로 정규화해 x축으로 사용
4. 필터 적용:
   - `baseline_capacity_ah >= 1.0` (파손/극단 cell 배제)
   - `0.6 <= soh_ratio <= 1.1` (보조배터리 실사용 범위)
5. `scipy.optimize.curve_fit`으로 `a·exp(-b·x) + c` 피팅

#### 상수
| 상수 | 값 | 적용 여부 |
|---|---|---|
| DC-DC 컨버터 효율 (`CONVERTER_EFFICIENCY`) | 0.85 | 응답의 `powerbank_usable_mah` 계산에 반영 |
| 스마트폰 출력 전압 (`OUTPUT_VOLTAGE`) | 5.0V | `estimated_full_charges` 관련 계산에 반영 |
| 셀 공칭 전압 (`NOMINAL_CELL_VOLTAGE_V`) | 3.7V | NASA/CALCE 정격 에너지 계산에만 사용 |

### 4-5. 추론 흐름

#### 단일 세션 (`/soh/predict`)
```
TelemetryPoint 리스트
    ↓ (fallback 분기)
표준 곡선의 x_median 지점에서 soh(x_median) 계산
    ↓ × 100, condition 라벨 매핑, 완충 횟수 계산
최종 응답 dict (confidence = "fallback", sessions_used = 0)
```

#### 다중 세션 (`/soh/predict/multi`)
```
UserSessionInput 리스트 (각 세션 = cycle_records + start_battery_level_pct)
    ↓ 각 세션별 필터링 (길이/에너지/잔량 조건)
유효 세션 목록
    ↓ 세션별 normalized_wh_per_pct = delivered_wh / (100 - start_battery_level_pct)
정규화된 값들의 시계열
    ↓ session_index 대비 선형 회귀 (np.polyfit)
유저 기울기
    ↓ 기울기 유의성 검사 (t-유사 통계량 임계값 2.0)
    ↓ 유의하지 않으면 fallback, 유의하면 개인화 계속
raw_ratio = (유저 상대 감소율) / (표준 상대 감소율)
rate = sqrt(raw_ratio)  ← log scale 완화
    ↓ 클립 [0.3, 3.0]
개인화된 x 지점 = x_median × rate, x_p95로 상한 클립
    ↓ soh(개인화 x)에서 최종 SOH 값 도출
상태 라벨 매핑, 완충 횟수 계산, confidence 등급 부여
    ↓
최종 응답 dict
```

### 4-6. 단위 변환 (Android → AI 서버)

| Android 수집값 | AI 서버 내부 표현 | 변환식 |
|---|---|---|
| `voltage_mv` (mV) | `voltage_v` (V) | ÷ 1000 |
| `current_ma` (mA) | `current_a` (A) | ÷ 1000 |
| `elapsed_ms` (ms) | `time_s` (s) | ÷ 1000 |
| `temperature_c` | `temperature_c` | 그대로 (nullable) |

### 4-7. 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | FastAPI |
| 피팅 · 수치 계산 | SciPy (`curve_fit`, `np.trapezoid`), NumPy |
| 데이터 처리 | Pandas, PyArrow (canonical parquet 적재) |
| 서버 시작 | `uvicorn soh_service.main:app --reload` (루트: `ai/` 디렉토리) |
| 모델 싱글턴 | 서버 시작 시 `standard_curve.json` 1회 로드, FastAPI dependency injection |

### 4-8. 로드맵 (미구현)

중간고사 이후 구현 예정. 현재 AI 서버에 존재하지 않으므로 호출하지 말 것.

#### `/soh/degradation/forecast` (계획)
SOH 이력을 입력받아 6개월 열화 경로와 EOL 도달 일자를 반환하는 엔드포인트. 유저 기울기를 장기 외삽으로 확장하고 95% 신뢰구간을 동반한다. EFC 계산에 `battery_sessions.powerbank_capacity_start_mah` / `powerbank_capacity_end_mah`의 SOC % 값을 사용한다.

**예상 요청**:
```json
{
  "soh_history": [
    {"measured_at": "2026-03-01", "soh_percentage": 95.0},
    {"measured_at": "2026-03-08", "soh_percentage": 92.3}
  ],
  "session_history": [
    {"date": "2026-03-01", "phone_soc_start": 10.0, "phone_soc_end": 90.0},
    {"date": "2026-03-08", "phone_soc_start": 20.0, "phone_soc_end": 85.0}
  ],
  "powerbank_capacity_mah": 10000,
  "phone_capacity_mah": 4000
}
```

**예상 응답**:
```json
{
  "is_sufficient": true,
  "forecast": [
    {"date": "2026-04-14", "soh_predicted": 91.5, "soh_lower": 88.2, "soh_upper": 94.8}
  ],
  "weekly_efc": 2.5,
  "current_efc": 15.0,
  "sessions_used": 12,
  "eol_date": "2026-10-15"
}
```

활성화 조건: `세션 ≥ 10회 AND 기간 ≥ 7일`. 미충족 시 `is_sufficient=false`, `forecast=[]`.

---

## 5. 구현 현황 분석

### 완료된 기능

| 영역 | 완료 항목 |
|---|---|
| AI 서버 | 곡선 피팅 기반 SOH 예측 파이프라인 (NASA 표준 곡선 + 유저 기울기 개인화) |
| AI 서버 | `/soh/predict`, `/soh/predict/multi`, `/soh/health` API |
| AI 서버 | NASA canonical artifact export 및 offline 표준 곡선 피팅 스크립트 |
| AI 서버 | 기울기 유의성 검사(t-통계) + log scale 완화로 노이즈 방어 |
| Backend | 세션 CRUD, 원시 데이터 수집, AI 연동, 결과 저장 전체 |
| Backend | 사용자 인증(회원가입/로그인), 사용자 CRUD |
| Android | 전체 화면 네비게이션 흐름 (Splash → Dashboard) |
| Android | BatteryManager 실시간 데이터 수집 (2초 주기) |
| Android | 기기 등록/조회/삭제 (SharedPreferences) |

### 미구현 / 향후 예정

| 영역 | 항목 |
|---|---|
| AI 서버 | CALCE 데이터셋 통합 및 cross-dataset 표준 곡선 재피팅 (중간고사 후) |
| AI 서버 | `/soh/degradation/forecast` (6개월 열화 예측, 4-8 참조) |
| Android | Backend REST API 실제 연동 (현재 로컬 저장만) |
| Android | 충전 세션 시작/종료 → Backend 전송 |
| Android | 원시 데이터 → Backend 업로드 로직 |
| Android | SOH 예측 결과 화면 표시 |
| Android | `BatteryDevice`의 `ageInMonths`, `chargeCount`, `healthPercentage`, `estimatedLifespan` |
| Android | `/sessions/start` 요청에 `start_battery_level_pct`, `/sessions/{id}/finish`에 `end_battery_level_pct` 추가 (개인화 예측용, 백엔드 스키마 확장 후) |
| Backend | `battery_sessions`에 `powerbank_capacity_start_mah` / `powerbank_capacity_end_mah` 실제 컬럼 추가 (3-3 주석 참조) |
| Backend | `battery_ai_results`에 신규 컬럼 5개 추가 + Alembic migration (3-3 주석 참조) |
| Backend | `SohPredictResponse`에 AI 신규 응답 필드 4개 Optional 추가 (3-2 주석 참조) |
| Backend | `ai_service.py::predict_soh_for_session()`에서 `capacity_ah` 필드 전송 제거 (선택) |
| Backend | 개인화 기능 활성화 시 `/soh/predict/multi` 호출 전환 |
| Backend | JWT 토큰 기반 인증 미들웨어 |
| Backend | Docker 컨테이너 구성 |

---

## 6. 주요 설정값 요약

| 설정 | 값 | 위치 |
|---|---|---|
| 에뮬레이터 AI 서버 주소 | `10.0.2.2:8000` | Backend `.env` 또는 Android 하드코딩 |
| 표준 곡선 아티팩트 | `artifacts/curve_fit/standard_curve.json` | 서버 시작 시 1회 로드 |
| DC-DC 컨버터 효율 | 0.85 | `ai/soh_service/core/config.py` |
| 스마트폰 출력 전압 | 5.0 V | `ai/soh_service/core/config.py` |
| 셀 공칭 전압 | 3.7 V | `ai/soh_service/curve_fit/fitter.py` |
| 기본 스마트폰 용량 | 4,000 mAh | `ai/soh_service/core/config.py` |
| 기본 보조배터리 용량 | 10,000 mAh | `ai/soh_service/core/config.py` |
| 최소 세션 길이 | 600초 (10분) | `ai/soh_service/curve_fit/predictor.py` |
| 최소 세션 전달 에너지 | 2 Wh | `ai/soh_service/curve_fit/predictor.py` |
| 시작 배터리 잔량 상한 | 85% | `ai/soh_service/curve_fit/predictor.py` |
| 채울 여지 하한 | 15%p | `ai/soh_service/curve_fit/predictor.py` |
| 개인화 최소 유효 세션 | 3개 | `ai/soh_service/curve_fit/predictor.py` |
| 기울기 유의성 임계값 | t_stat = 2.0 | `ai/soh_service/curve_fit/predictor.py` |
| `degradation_rate_ratio` 클립 | `[0.3, 3.0]` | `ai/soh_service/curve_fit/predictor.py` |
| 최대 세션 누적 수 | 20개 | `ai/soh_service/core/config.py` |
| Android 데이터 수집 주기 | 2,000ms | `DashboardViewModel.kt` |
| Android minSdk | 26 (Android 8.0) | `android/app/build.gradle.kts` |
