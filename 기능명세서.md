# BatteryInsight Android — 기능명세서

---

## 1. 시스템 아키텍처 개요

프로젝트는 **3-tier** 구조로 분리되어 있다.

```
Android App (Kotlin)
    ↓ REST API
Backend Server (FastAPI + SQLAlchemy)
    ↓ REST API (HTTP)
AI Server (FastAPI + PyTorch LSTM)
```

| 레이어 | 경로 | 핵심 기술 |
|---|---|---|
| Android 앱 | `android/` | Kotlin, Jetpack Compose, Navigation, BatteryManager API |
| Backend 서버 | `backend/` | FastAPI, SQLAlchemy, PostgreSQL, passlib(bcrypt) |
| AI 서버 | `ai/soh_service/` | FastAPI, PyTorch(HierarchicalLSTM), NASA+CALCE 학습 모델 |

---

## 2. Android 앱

### 2-1. 화면 구성 및 네비게이션

`MainActivity.kt`에서 Jetpack Navigation Compose로 전체 흐름을 관리한다.

```
splash
  ├─ (로그인 상태 O) → home
  └─ (로그인 상태 X) → login
                          └─ signup
home
  ├─ 기기 선택 → dashboard/{deviceNickname}
  └─ 기기 추가 → landing → dashboard/{deviceNickname}
```

| 화면 | 라우트 | 주요 역할 |
|---|---|---|
| SplashScreen | `splash` | 자동 로그인 여부 확인 후 home/login 분기 |
| LoginScreen | `login` | 사용자 로그인 처리 |
| SignupScreen | `signup` | 회원가입 처리 |
| HomeScreen | `home` | 등록 기기 목록 표시 / 기기 삭제 / 로그아웃 |
| LandingScreen | `landing` | 신규 보조배터리 등록 (정보 입력) |
| DashboardScreen | `dashboard/{deviceNickname}` | 실시간 배터리 모니터링 |

### 2-2. 데이터 모델

#### BatteryDevice
사용자가 등록한 보조배터리 정보.

| 필드 | 타입 | 설명 |
|---|---|---|
| `brand` | String | 제조사 (예: Anker, Samsung) |
| `nickname` | String | 사용자 지정 별칭 (PK 역할, 필수) |
| `capacity` | Int | 정격 용량 (mAh, 0 초과 필수) |
| `manufactureDate` | String | 제조년월 (형식: YYYY-MM) |

> 향후 추가 예정: `ageInMonths`, `chargeCount`, `healthPercentage`, `estimatedLifespan`

#### BatteryStatus
실시간 배터리 상태 스냅샷.

| 필드 | 타입 | 설명 |
|---|---|---|
| `soc` | Int | 잔량 (%) |
| `voltage` | Float | 전압 (V) |
| `current` | Float | 전류 (mA), 충전 중 양수 |
| `temperature` | Float | 온도 (°C) |
| `isCharging` | Boolean | 충전 중 여부 |
| `remainingTime` | Long | 완충 예상 시간 (분), 미산출 시 -1 |

### 2-3. DashboardViewModel — 실시간 데이터 수집

Android `BatteryManager` API를 통해 **2초 주기**로 하드웨어 센서 데이터를 수집한다.

| 수집 항목 | Android API | 변환 |
|---|---|---|
| 잔량 (SOC) | `EXTRA_LEVEL / EXTRA_SCALE` | level / scale × 100 |
| 전압 | `EXTRA_VOLTAGE` | mV ÷ 1000 → V |
| 온도 | `EXTRA_TEMPERATURE` | 0.1°C 단위 ÷ 10 → °C |
| 전류 | `BATTERY_PROPERTY_CURRENT_NOW` | μA ÷ 1000 → mA |
| 충전 상태 | `EXTRA_STATUS` | CHARGING / FULL → true |
| 완충 예상 시간 | `computeChargeTimeRemaining()` | ms ÷ 60000 → 분 (API 28+) |

- 완충 예상 시간이 -1이면 UI에서 "계산 중..." 표시
- `viewModelScope.launch` + `delay(2000)` 루프로 구현

### 2-4. 저장소 구조

| 클래스 | 역할 |
|---|---|
| `PreferenceManager` | 기기 목록(BatteryDevice) SharedPreferences 관리 |
| `UserManager` | 로그인 상태, 사용자 정보 SharedPreferences 관리 |
| `BatteryRepository` (interface) | 배터리 상태/기기 CRUD 추상화 |
| `MockBatteryRepository` | 테스트용 Mock 구현체 |

### 2-5. 기술 스택 (build.gradle.kts)

| 항목 | 버전/값 |
|---|---|
| compileSdk / targetSdk | 35 |
| minSdk | 26 (Android 8.0) |
| Compose BOM | 최신 |
| Navigation Compose | 2.8.9 |
| ViewModel Compose | 2.8.7 |
| kotlinx-serialization-json | 1.6.2 |

---

## 3. Backend 서버

### 3-1. API 엔드포인트 목록

Base URL: `/api/v1`

#### 인증 (`/auth`)

| 엔드포인트 | 메서드 | 요청 | 응답 | 설명 |
|---|---|---|---|---|
| `/auth/signup` | POST | `SignUpRequest` | `SignUpResponse` | 회원가입 |
| `/auth/login` | POST | `LoginRequest` | `LoginResponse` | 로그인 |

#### 사용자 (`/users`)

| 엔드포인트 | 메서드 | 설명 |
|---|---|---|
| `/users` | GET | 사용자 목록 조회 (skip, limit 파라미터) |
| `/users/{user_id}` | GET | 단일 사용자 조회 |
| `/users/{user_id}` | PATCH | 사용자 정보 수정 |
| `/users/{user_id}` | DELETE | 사용자 삭제 |

#### 충전 세션 (`/sessions`)

| 엔드포인트 | 메서드 | 요청 | 응답 | 설명 |
|---|---|---|---|---|
| `/sessions/start` | POST | `SessionStartRequest` | `SessionStartResponse` | 세션 시작, session_id 발급 |
| `/sessions/{id}/finish` | POST | `SessionFinishRequest` | `SessionFinishResponse` | 세션 종료, capacity_ah 기록 |
| `/sessions/{id}/raw` | POST | `RawUploadRequest` | - | 원시 배터리 데이터 업로드 |
| `/sessions/{id}/predict-soh` | POST | - | `SohPredictResponse` | AI 서버에 SOH 예측 요청 |
| `/sessions/{id}/result` | GET | - | `SessionResultResponse` | 세션 분석 결과 조회 |

#### 헬스체크

| 엔드포인트 | 메서드 | 설명 |
|---|---|---|
| `/health` | GET | 서버 상태 확인 |

### 3-2. 주요 스키마

#### SessionStartRequest
```
user_id, device_model, android_api_level,
powerbank_id (optional), cable_id (optional),
phone_capacity_mah (default: 4000),
powerbank_capacity_mah (default: 10000),
session_start_ts
```

#### RawDataPoint
```
timestamp, voltage_mv, current_ma, temperature_c, elapsed_ms,
battery_level (optional), battery_status (optional), screen_state (optional)
```
- `elapsed_ms`는 단조 증가 검증 (validator 포함)

#### SohPredictResponse
```
soh_percentage, condition, estimated_full_charges,
powerbank_usable_mah, smartphone_received_mah, mean_temperature_c
```

### 3-3. DB 스키마

#### `battery_sessions`
| 컬럼 | 타입 | 설명 |
|---|---|---|
| `session_id` | VARCHAR(50) | 고유 세션 식별자 (PK 역할) |
| `user_id` | VARCHAR(100) | 사용자 ID |
| `device_model` | VARCHAR(100) | 스마트폰 기종 |
| `android_api_level` | INT | 안드로이드 API 레벨 |
| `powerbank_id` | VARCHAR(100) | 보조배터리 식별자 (nullable) |
| `cable_id` | VARCHAR(100) | 케이블 식별자 (nullable) |
| `phone_capacity_mah` | INT | 스마트폰 배터리 용량 |
| `powerbank_capacity_mah` | INT | 보조배터리 정격 용량 |
| `session_start_ts` | DATETIME | 세션 시작 시각 |
| `session_end_ts` | DATETIME | 세션 종료 시각 (nullable) |
| `capacity_ah` | FLOAT | 총 방전 용량 (nullable) |
| `status` | VARCHAR(30) | in_progress / finished |

#### `battery_raw_points`
| 컬럼 | 타입 | 설명 |
|---|---|---|
| `session_id` | VARCHAR(50) | FK → battery_sessions |
| `timestamp` | DATETIME | 측정 시각 |
| `voltage_mv` | FLOAT | 전압 (mV) |
| `current_ma` | FLOAT | 전류 (mA) |
| `temperature_c` | FLOAT | 온도 (°C) |
| `elapsed_ms` | FLOAT | 세션 시작 후 경과 시간 (ms) |
| `battery_level` | INT | 배터리 잔량 % (nullable) |
| `battery_status` | VARCHAR(50) | 배터리 상태 문자열 (nullable) |
| `screen_state` | BOOLEAN | 화면 on/off (nullable) |

> UNIQUE 제약: `(session_id, elapsed_ms)`

#### `battery_ai_results`
| 컬럼 | 타입 | 설명 |
|---|---|---|
| `session_id` | VARCHAR(50) | FK → battery_sessions (unique) |
| `soh_percentage` | FLOAT | SOH 예측값 (%) |
| `condition` | VARCHAR(50) | 상태 라벨 |
| `estimated_full_charges` | FLOAT | 완충 가능 횟수 추정 |
| `powerbank_usable_mah` | FLOAT | 실사용 가능 용량 |
| `smartphone_received_mah` | FLOAT | 스마트폰 수신 용량 추정 |
| `mean_temperature_c` | FLOAT | 세션 평균 온도 |
| `raw_response_json` | TEXT | AI 서버 원본 응답 JSON |

#### `users`
| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | INT | PK |
| `name` | VARCHAR(50) | 이름 |
| `username` | VARCHAR(50) | 로그인 ID (unique) |
| `password_hash` | VARCHAR(255) | bcrypt 해시 |

### 3-4. 서비스 로직 요약

#### 세션 흐름
1. `POST /sessions/start` → `session_id` 발급 (UUID 기반, `sess_` prefix)
2. `POST /sessions/{id}/raw` → 원시 데이터 누적 저장 (최소 10포인트 필요)
3. `POST /sessions/{id}/finish` → `capacity_ah` 기록, status → `finished`
4. `POST /sessions/{id}/predict-soh` → AI 서버 `/soh/predict` 호출 → 결과 DB 저장

#### AI 서버 호출 조건
- `status == "finished"`
- `capacity_ah != null`
- `raw_points >= 10개`

### 3-5. 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | FastAPI |
| ORM | SQLAlchemy |
| DB 드라이버 | psycopg2-binary (PostgreSQL) |
| 인증 | passlib[bcrypt] |
| HTTP 클라이언트 | requests |
| 설정 관리 | pydantic-settings |

---

## 4. AI 서버

### 4-1. API 엔드포인트

Base URL: `/soh`

| 엔드포인트 | 메서드 | 설명 |
|---|---|---|
| `/soh/predict` | POST | 단일 세션 SOH 예측 |
| `/soh/predict/multi` | POST | 다중 세션 누적 SOH 예측 |
| `/soh/health` | GET | 모델 로드 상태 확인 |

### 4-2. 요청/응답 스키마

#### `/soh/predict` 요청
```json
{
  "cycle_records": [
    {
      "voltage_mv": 4200.0,
      "current_ma": 1500.0,
      "temperature_c": 25.0,
      "elapsed_ms": 0.0
    }
  ],
  "powerbank_capacity_mah": 10000,
  "phone_capacity_mah": 4000
}
```
- `cycle_records` 최소 10개 필요
- `temperature_c`는 nullable

#### `/soh/predict/multi` 요청
```json
{
  "sessions": [ [/* CycleRecord 배열 */], [/* ...*/] ],
  "powerbank_capacity_mah": 10000,
  "phone_capacity_mah": 4000
}
```
- 10분(600초) 미만 세션은 자동 제외
- 최대 20개 세션 사용 (초과 시 최근 20개)

#### 공통 응답
```json
{
  "soh_percentage": 85.23,
  "condition": "양호",
  "estimated_full_charges": 2.13,
  "powerbank_usable_mah": 8523.0,
  "mean_temperature_c": 26.4
}
```

#### `/soh/predict/multi` 추가 응답 필드
```json
{
  "sessions_used": 5
}
```

#### condition 라벨 기준
| SOH 범위 | 라벨 |
|---|---|
| 90% 이상 | 우수 |
| 80% ~ 90% 미만 | 양호 |
| 70% ~ 80% 미만 | 주의 |
| 70% 미만 | 교체 권장 |

### 4-3. 모델 구조

| 항목 | 값 |
|---|---|
| 모델 | `HierarchicalLstmRegressor` (계층형 LSTM) |
| 학습 데이터 | NASA Battery Dataset + CALCE Battery Dataset (cross-training) |
| 체크포인트 | `artifacts/checkpoints/lstm/nasa_calce/v3/nc_v3_s1_cosine_ep30/` |
| 입력 단위 | 슬라이딩 윈도우 (600초/창, 20초 리샘플링) |
| 최대 세션 누적 | 20개 (N_MAX_SESSIONS) |
| 출력 | SOH 비율 (0.0 ~ 1.0, clamp 적용) |

**체크포인트 파일 구성:**
```
config.json               — 모델 하이퍼파라미터
normalization_stats.json  — 피처 정규화 통계
transform_metadata.json   — 윈도우 정책 및 변환 설정
best.pt                   — 학습된 가중치 (PyTorch)
```

### 4-4. 전처리 파이프라인

#### NASA → 스마트폰 환경 변환
| 변환 항목 | 처리 |
|---|---|
| DC-DC 컨버터 효율 | × 0.85 (`CONVERTER_EFFICIENCY`) |
| 출력 전압 고정 | 5.0V (`OUTPUT_VOLTAGE`) 기준으로 스케일링 |
| 스마트폰 수신 전력 | `energy_wh × 0.85 / 5.0 × 1000` → mAh |

#### 피처 목록 (`extract_features`)
```
mean_voltage, std_voltage, min_voltage,
mean_current, max_current,
mean_temperature, max_temperature, std_temperature,
discharge_duration_s, discharged_ah, energy_wh,
smartphone_received_wh, smartphone_received_mah,
voltage_drop_rate, voltage_start, voltage_end
```

#### 타겟 (학습 시만 사용)
```
soh, estimated_full_charges, powerbank_usable_mah
```

### 4-5. 추론 흐름

```
TelemetryPoint 리스트 (Android BatteryManager 수집값)
    ↓ _build_canonical_sample()
CanonicalSequenceSample
    ↓ build_windowed_sequence_bundle()
WindowedSequenceBundle (600초 단위 슬라이딩 윈도우)
    ↓ _normalize() → torch.Tensor [1, n_windows, steps, features]
HierarchicalLstmRegressor.forward()
    ↓ torch.clamp(prediction, 0.0, 1.0)
SOH 비율 (float)
    ↓ × 100, condition 라벨 매핑, 완충 횟수 계산
최종 응답 dict
```

### 4-6. 단위 변환 (Android → AI 서버)

| Android 수집값 | AI 서버 입력값 | 변환식 |
|---|---|---|
| `voltage_mv` (mV) | `voltage_v` (V) | ÷ 1000 |
| `current_ma` (mA) | `current_a` (A) | ÷ 1000 |
| `elapsed_ms` (ms) | `time_s` (s) | ÷ 1000 |
| `temperature_c` | `temperature_c` | 그대로 (nullable) |

### 4-7. 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | FastAPI |
| 딥러닝 | PyTorch |
| 수치 연산 | NumPy, SciPy (trapezoid 적분) |
| 데이터 처리 | Pandas |
| 서버 시작 | `uvicorn soh_service.main:app --reload` (루트: `ai/` 디렉토리) |
| 모델 싱글턴 | 서버 시작 시 1회 로드, FastAPI dependency injection 방식 유지 |

---

## 5. 구현 현황 분석

### 완료된 기능

| 영역 | 완료 항목 |
|---|---|
| AI 서버 | LSTM 모델 학습 및 추론 파이프라인 전체 |
| AI 서버 | `/soh/predict`, `/soh/predict/multi`, `/soh/health` API |
| AI 서버 | NASA+CALCE 크로스 학습, 전처리, 윈도잉, 정규화 |
| Backend | 세션 CRUD, 원시 데이터 수집, AI 연동, 결과 저장 전체 |
| Backend | 사용자 인증(회원가입/로그인), 사용자 CRUD |
| Android | 전체 화면 네비게이션 흐름 (Splash → Dashboard) |
| Android | BatteryManager 실시간 데이터 수집 (2초 주기) |
| Android | 기기 등록/조회/삭제 (SharedPreferences) |

### 미구현 / 향후 예정

| 영역 | 항목 |
|---|---|
| Android | Backend REST API 실제 연동 (현재 로컬 저장만) |
| Android | 충전 세션 시작/종료 → Backend 전송 |
| Android | 원시 데이터 → Backend 업로드 로직 |
| Android | SOH 예측 결과 화면 표시 |
| Android | `BatteryDevice`의 `ageInMonths`, `chargeCount`, `healthPercentage`, `estimatedLifespan` |
| Backend | JWT 토큰 기반 인증 미들웨어 |
| Backend | Docker 컨테이너 구성 |

---

## 6. 주요 설정값 요약

| 설정 | 값 | 위치 |
|---|---|---|
| 에뮬레이터 AI 서버 주소 | `10.0.2.2:8000` | Backend `.env` 또는 Android 하드코딩 |
| DC-DC 컨버터 효율 | 0.85 | `ai/soh_service/core/config.py` |
| 스마트폰 출력 전압 | 5.0 V | `ai/soh_service/core/config.py` |
| 기본 스마트폰 용량 | 4,000 mAh | `ai/soh_service/core/config.py` |
| 기본 보조배터리 용량 | 10,000 mAh | `ai/soh_service/core/config.py` |
| 최소 세션 길이 | 600초 (10분) | `inference/predictor.py` |
| 최대 세션 누적 수 | 20개 | `ai/soh_service/core/config.py` |
| Android 데이터 수집 주기 | 2,000ms | `DashboardViewModel.kt` |
| Android minSdk | 26 (Android 8.0) | `android/app/build.gradle.kts` |
